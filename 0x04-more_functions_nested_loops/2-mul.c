#include "main.h"
#include <stdio.h>

/**
 * mul - Multiply two integers.
 * @a: integer to be multiplied with b
 * @b : integer to be multipled with a
 * Return: Return the result of the multiplication
 */
int mul(int a, int b)
{
	return (a * b);
}
